# Bayesian Analysis v10 — 実行結果分析レポート

**実行日時**: 2026-03-09 15:31:17  
**プログラム**: `bayesian_v10_polariton_priority.py`  
**結果ディレクトリ**: `bayesian_v9_results_20260309_153117/`  
**サンプラー**: SMC (draws=10000, chains=16, total=160,000 samples)

---

## 1. 概要

v10.0 では v9.0 のトレードオフ問題（共振器スペクトル全体の StudentT 尤度が結合定数 a を歪める）を解決するため、共振器スペクトル形状尤度を除去し、ピーク位置と FWHM の点制約のみに変更した。

### サンプリング結果サマリー

| メトリクス | H-form | B-form |
|-----------|--------|--------|
| 平均 R-hat | 1.001 | 1.007 |
| 平均 ESS | 43,791 | 11,062 |
| g-factor | 1.889 | 2.015 |
| a (結合定数) | 4.782 | 4.173 |
| B₄ (mK) | +49.0 | −64.1 |
| B₆ (mK) | −0.07 (HDI: −223〜+231) | +1.62 (HDI: −180〜+283) |
| ε_bg | 13.97 | 14.03 |
| γ平均 (THz) | 0.043 | 0.051 |

### モデル選択結果

| 基準 | 推奨モデル | 値 |
|------|----------|-----|
| ESS 比較 | **H-form** | 43,791 vs 11,062 |
| ベイズファクター (SMC) | **B-form** | log₁₀(BF) = −20.2 (強い証拠) |

→ **2つの基準が矛盾**しており、モデル選択の結論が不明確。

---

## 2. 問題点の詳細

### 2.1 【Critical】共振器ピーク位置マッチング失敗 (7.0T, 9.0T)

**症状**: 7.0T と 9.0T(H-form では 9.0T も)でΔf > 240 GHz という巨大なピーク位置誤差が発生。

| データセット | H-form Δf | B-form Δf |
|-------------|-----------|-----------|
| 7.0T | 244.3 GHz | 244.3 GHz |
| 9.0T | 249.1 GHz | 244.3 GHz |
| その他 | 4.9〜14.7 GHz | 4.9〜14.7 GHz |

**原因**: `PEAK_MATCH_MAX_DISTANCE = 0.12 THz` のマッチング距離制約により、予測ピークと観測ピークが対応付けられず、ペナルティ値 (`obs_freq + 0.1`) が使用される。マッチング失敗時のペナルティは `obs_freq + 0.1` THz のオフセットだが、これは実際の不整合度を反映せず、サンプラーに適切な勾配情報を提供できない。

**影響**: 共振器ピーク位置制約が 7T/9T で事実上無効化され、eps_bg の推定精度が低下。

### 2.2 【Critical】共振器領域の RMSE が高い

**症状**: 全データセットで共振器領域 RMSE が 0.13〜0.18 と高い。

| 領域 | H-form RMSE 範囲 | B-form RMSE 範囲 |
|------|------------------|------------------|
| ポラリトン | 0.023〜0.150 | 0.046〜0.152 |
| 共振器 | 0.137〜0.177 | 0.132〜0.183 |

**原因**: v10.0 で共振器スペクトル形状尤度を除去したため、共振器領域のフィッティングがピーク位置・FWHM の点制約のみに依存。スペクトル全体の包絡線形状（ピーク高さ、非対称性）に対する制約がない。

**視覚的所見**: 後方予測スペクトルプロットでは、共振器領域で予測ピークが観測よりも鋭く高い（ピーク高さの過大予測、裾の過小予測）。

### 2.3 【Major】B₆ パラメータの識別不能

**症状**: B₆ の事後分布が事前分布とほぼ同一で、データから情報が得られていない。

| 形式 | B₆ mean (mK) | B₆ sd (mK) | 94% HDI (mK) |
|------|-------------|------------|---------------|
| H-form | −0.07 | 121.1 | −223〜+231 |
| B-form | +1.62 | 123.0 | −180〜+283 |

**原因**: 現在の事前分布 `Normal(μ=0, σ=5mK)` に対してスケーリング `SCALING_FACTORS['B6']=25000` を適用すると、実効的な事前分布が `Normal(μ=0, σ=125mK)` 相当となり非常に広い。0.1〜1 THz の周波数範囲ではB₆の寄与が小さく、データによる制約が不十分。

### 2.4 【Major】B-form の階層γパラメータで ESS 低下

**症状**: B-form の階層ハイパーパラメータの ESS が低い。

| パラメータ | ESS_bulk | r_hat |
|-----------|---------|-------|
| log_gamma_sd | 372 | 1.03 |
| gamma_raw[1] | 427 | 1.02 |
| log_gamma_mu | 648 | 1.02 |

**原因**: B-form の γ₄ が 0.183 THz と他の γ（0.014〜0.069 THz）と大きく異なり、階層モデルの共通分布からの逸脱が大きい。SMC サンプラーが log_gamma_sd を探索する際に、γ₄ を包含するために広い分散が必要となるが、他の γ はそれを許容しない、というトレードオフでサンプリング効率が低下。

### 2.5 【Major】バージョンラベルの不整合

| 箇所 | 表記 |
|------|------|
| ファイル名 | `bayesian_v10_polariton_priority.py` |
| docstring | v10.0 |
| `main()` print文 | "v9.0" |
| 結果ディレクトリ名 | `bayesian_v9_results_*` |
| プロットタイトル | "v9.0 Multi Cavity Peak", "v8.0" |
| `build_pymc_model` docstring | "v8.0 複合尤度モデル" |

→ v8, v9, v10 の3バージョンが混在しており、再現性の観点で深刻。

### 2.6 【Medium】gamma_std_scaled の計算誤り

**現在のコード** (L1204-1205):
```python
pm.Deterministic('gamma_std_scaled',
    log_gamma_sd * SCALING_FACTORS['gamma'])
```

**問題**: `log_gamma_sd` は対数空間の標準偏差（無次元）であり、これに線形空間のスケーリング係数 (100) を掛けても物理的に意味のある量にならない。B-form で `gamma_std_scaled = 61.7 GHz` と報告されているが、これは対数空間 SD = 0.617 × 100 に過ぎず、実際の γ の変動幅を反映していない。

**修正案**: 線形空間での γ 標準偏差を計算する:
```python
gamma_std_linear = pt.exp(log_gamma_mu) * pt.sqrt(pt.exp(log_gamma_sd**2) - 1)
```

### 2.7 【Medium】RMSE メトリクスが保存されていない

`plot_posterior_predictive_spectra()` 内で領域別 RMSE を計算してプロットタイトルに表示しているが、これらの数値をファイルに保存していない。バージョン間の定量比較が困難。

### 2.8 【Medium】ポラリトン−共振器間ギャップ領域 (0.36–0.45 THz)

`POLARITON_UPPER = 0.361505` と `CAVITY_LOWER = 0.45` の間の約 90 GHz のギャップ領域では、weight=0.01（背景扱い）の弱い制約しかかからない。この領域にも物理的に意味のある情報（反交差の裾など）が含まれる可能性がある。

---

## 3. 改善提案

| # | 優先度 | 改善内容 | 対応 Issue |
|---|--------|---------|-----------|
| 1 | Critical | ピークマッチング失敗時のフォールバック改善 | #1 |
| 2 | Critical | 共振器領域の軽量スペクトル制約を追加 | #2 |
| 3 | Major | B₆ 事前分布の狭小化 | #3 |
| 4 | Major | 収束診断の自動警告システム | #4 |
| 5 | Major | バージョンラベルの統一 | #5 |
| 6 | Medium | gamma_std_scaled 計算の修正 | #6 |
| 7 | Medium | RMSE メトリクスの出力ファイル保存 | #5 |

---

## 4. パラメータ推定値の物理的妥当性

### g-factor
- H-form: 1.889 → Gd³⁺ 理論値 (1.99) より低い
- B-form: 2.015 → 理論値に近く妥当

### 結合定数 a
- H-form: 4.782, B-form: 4.173 → いずれも妥当な範囲

### 結晶場パラメータ B₄
- H-form: +49.0 mK, B-form: −64.1 mK → **符号が逆転**
- 物理的に両方が正しいことはありえず、モデル形式の選択に依存した系統誤差が存在

### γ (線幅)
- γ₁〜γ₃: 両形式とも 0.014〜0.035 THz で安定
- γ₄: B-form で 0.183 THz と異常に大きい（物理的に疑問）
- γ₅〜γ₇: 0.05〜0.07 THz で比較的安定だが不確実性が大きい
