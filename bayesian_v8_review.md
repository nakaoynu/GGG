# Bayesian v8 Mixed Likelihood フィッティング結果レビュー

## 1. 概要

**対象プログラム**: `bayesian_v8_mixed_likelihood.py` (v8.0)  
**結果ディレクトリ**: `bayesian_v8_results_20260304_191835/`  
**手法**: SMC (Sequential Monte Carlo) サンプリング, 10,000 draws × 16 chains = 160,000 サンプル  
**尤度構造**: 混合尤度 — ポラリトン領域は透過スペクトル形状 (StudentT, ν=4)、キャビティ領域は FWHM のみ (StudentT, ν=4)

---

## 2. 収束診断

| 指標 | H-form | B-form |
|------|--------|--------|
| 平均 R-hat | 1.0000 | 1.0005 |
| 平均 ESS | 53,657 | 33,358 |
| 収束判定 | ✅ 良好 | ✅ 良好 |

両フォームとも R-hat ≈ 1.0 であり、収束は達成されている。B-form の ESS が H-form より低い点は、パラメータ空間のより複雑な構造を示唆する。

**ベイズ因子計算**: `float()` 呼び出しの次元エラーにより失敗 (Bug #3)。

---

## 3. フィッティング品質の定性評価

### 3.1 ポラリトン領域 (f < 0.361 THz)

事後予測スペクトルにおいて、ポラリトン領域の主要な特徴（反交差構造、ピーク分裂）は**概ね再現**されている。ただし、系統的なオフセットが認められる。これは透過スペクトル形状の直接フィッティングが有効に機能していることを示す。

### 3.2 キャビティ領域 (f > 0.45 THz) — ⚠️ 重大な問題

高次共振器モードのピーク**位置**が観測値から系統的にずれている。FWHM は概ね再現されているが（ユーザー所見と一致）、ピーク位置の再現に失敗している。

**根本原因**: キャビティ領域の尤度関数が **FWHM のみ** を対象としており、**ピーク周波数の情報が一切含まれていない**。モデルにはキャビティピーク位置を正しい周波数に合わせるインセンティブが存在しない。

### 3.3 FWHM 予測の問題

H-form の FWHM 予測値は ~71 GHz にほぼ固定されており、観測値の 70–83 GHz の変動を捉えられていない。FWHM のみの尤度では制約が不十分であることを示す。

---

## 4. 再現度の定量的評価手法

ユーザーの要望に応え、以下の定量指標を提案する。

### 4.1 領域別 RMSE (Root Mean Square Error)

$$\text{RMSE}_{\text{region}} = \sqrt{\frac{1}{N_{\text{region}}} \sum_{i \in \text{region}} (T_{\text{pred}}(f_i) - T_{\text{obs}}(f_i))^2}$$

- ポラリトン領域 RMSE: f < 0.361 THz のデータ点のみで計算
- キャビティ領域 RMSE: f > 0.45 THz のデータ点のみで計算
- 全体 RMSE: 既存の値 (0.13–0.18)

### 4.2 ピーク位置誤差 (Peak Position Error)

$$\Delta f_{\text{peak}} = |f_{\text{pred,peak}} - f_{\text{obs,peak}}|$$

各キャビティモードのピーク周波数について、予測値と観測値の差を GHz 単位で報告。

### 4.3 FWHM 誤差

$$\Delta \text{FWHM} = |\text{FWHM}_{\text{pred}} - \text{FWHM}_{\text{obs}}|$$

各データセットごとの FWHM 再現精度を GHz 単位で報告。

### 4.4 領域別 χ² (Reduced Chi-Square)

$$\chi^2_{\nu,\text{region}} = \frac{1}{N_{\text{region}} - k} \sum_{i \in \text{region}} \frac{(T_{\text{pred}}(f_i) - T_{\text{obs}}(f_i))^2}{\sigma_i^2}$$

χ²_ν ≈ 1 が理想的。χ²_ν >> 1 は under-fitting、χ²_ν << 1 は over-fitting を示す。

### 4.5 スペクトル重なり積分 (Spectral Overlap)

$$\text{SO} = 1 - \frac{\int |T_{\text{pred}}(f) - T_{\text{obs}}(f)| df}{\int df}$$

SO = 1 が完全一致。形状の全体的な一致度を測る指標。

---

## 5. パラメータ推定結果

### 5.1 H-form

| パラメータ | 平均値 | 単位 | HDI 94% | 備考 |
|-----------|--------|------|---------|------|
| g | 1.882 | — | — | 良好な制約 |
| a | 5.292 | — | — | 良好な制約 |
| B4 | 0.0101 (≈17 mK) | cm⁻¹ | — | やや広い |
| B6 | -0.000116 (≈-2.9 mK) | cm⁻¹ | [-238, +239] mK | ⚠️ 本質的に無制約 |
| ε | 14.651 | — | — | 良好な制約 |
| γ₁–γ₇ | 0.016–0.033 | THz | — | 比較的均一 |

### 5.2 B-form

| パラメータ | 平均値 | 単位 | HDI 94% | 備考 |
|-----------|--------|------|---------|------|
| g | 2.021 | — | — | 良好な制約 |
| a | 3.866 | — | — | 良好な制約 |
| B4 | -0.070 (≈-117 mK) | cm⁻¹ | — | H-form より制約良好 |
| B6 | 0.0019 (≈48 mK) | cm⁻¹ | — | ⚠️ 広い |
| ε | 14.636 | — | — | 良好な制約 |
| γ₅ | 0.174 | THz (17.4 GHz) | [1.5, 33.3] GHz | ⚠️ 異常に大きく不確定 |

### 5.3 パラメータ識別性の問題

- **B6 が本質的に無制約**: 両フォームで事後分布が事前分布のほぼ全域にわたる。B6 は現在のデータセットからは識別不能な可能性がある。
- **B-form γ₅–γ₇ の広い事後分布**: 高次遷移の減衰率が十分に制約されていない。

---

## 6. 同定された問題点

### Issue #1: キャビティピーク位置が尤度関数に含まれていない 🔴 Critical

**深刻度**: Critical  
**影響**: キャビティ領域のピーク位置再現が不可能  
**場所**: `bayesian_v8_mixed_likelihood.py` lines 972–1112 (モデル構築部)

現在の実装ではキャビティ領域に対して FWHM のみの尤度を使用している。ピーク周波数の情報が尤度に含まれないため、モデルはキャビティピークの位置を合わせる動機を持たない。

**推奨修正**: キャビティ領域の尤度にピーク位置マッチングを追加する。具体的には、予測ピーク位置と観測ピーク位置の差に対する StudentT 尤度項を追加。

### Issue #2: FWHM フォールバックがモデル失敗を隠蔽 🟡 Major

**深刻度**: Major  
**場所**: lines 559–561

`compute_fwhm_representative` が None を返す場合、フォールバックとして観測値をそのまま使用（残差=0）する実装になっている。これはモデルが妥当な予測を出せない場合でも、尤度ペナルティが 0 になることを意味し、推定結果にバイアスを導入する。

**推奨修正**: フォールバック時に大きなペナルティ（例: 観測 FWHM の 2 倍を返す）を課すか、`pt.switch` で明示的なペナルティ値を設定する。

### Issue #3: ベイズ因子計算のバグ 🟡 Major

**深刻度**: Major  
**場所**: line 618

```python
float(trace.sample_stats["log_marginal_likelihood"])  # 多次元配列に float() → エラー
```

**推奨修正**:
```python
trace.sample_stats["log_marginal_likelihood"].values.flatten().mean()
```

### Issue #4: 領域別定量指標の欠如 🟡 Major

**深刻度**: Major  
**場所**: lines 650–780 (プロット関数)

現在は全体 RMSE のみ計算・表示しているが、ポラリトン領域とキャビティ領域を区別した定量指標がない。ユーザーの定性的観察（「ポラリトンは再現、キャビティは失敗」）を定量的に裏付けるメトリクスが必要。

**推奨修正**: 領域別 RMSE、ピーク位置誤差、FWHM 誤差を計算・表示する関数を追加。

### Issue #5: FWHM 予測が~71 GHz に固着 🟠 Moderate

**深刻度**: Moderate  
**影響**: キャビティモードの温度・磁場依存性の再現不足

FWHM のみの尤度では制約が不十分。Issue #1 の修正（ピーク位置追加）により、結晶場パラメータの制約が改善され、間接的に FWHM 予測も改善される可能性がある。

### Issue #6: Min-max 正規化が絶対スケールを破壊 🟠 Moderate

**深刻度**: Moderate  
**場所**: lines 278–282

```python
t_min = trans.min()
t_max = trans.max()
trans_normalized = (trans - t_min) / (t_max - t_min + 1e-10)
```

各スペクトルを [0,1] に独立に正規化するため、データセット間の絶対透過率の比較が不可能になり、見かけ上のピーク位置がシフトする可能性がある。

**推奨修正**: 正規化をオプション化し、オフ時は元の透過率スケールを使用。

### Issue #7: B6 の識別不能性 🔵 Info

**深刻度**: Info  
**影響**: パラメータ空間の無駄な探索

B6 が実質的に無制約であれば、固定値として扱うか、より情報的な事前分布を使用することを検討すべき。

---

## 7. 改善の優先順位

| 優先度 | Issue | 期待される改善 |
|--------|-------|---------------|
| 1 | #1 キャビティピーク位置 | キャビティ領域のピーク位置再現が根本的に改善 |
| 2 | #3 ベイズ因子バグ | モデル比較が可能に |
| 3 | #4 領域別メトリクス | 改善の定量的評価が可能に |
| 4 | #2 FWHM フォールバック | 推定のバイアス除去 |
| 5 | #6 正規化オプション | データの一貫性向上 |
| 6 | #7 B6 固定/制約 | サンプリング効率向上 |

---

## 8. 結論

v8 mixed likelihood アプローチはポラリトン領域の再現において有効性を示す一方、**キャビティ領域のピーク位置情報が尤度関数に含まれないという根本的な設計上の欠陥**が高次共振器モードの再現失敗の直接原因である。この問題の解決が最優先課題であり、キャビティ尤度にピーク位置マッチングを追加することで大幅な改善が期待される。

領域別 RMSE やピーク位置誤差などの定量指標の導入により、改善の効果を客観的に測定可能となる。
